<p id="notice"><%= notice %></p>

<% if @photo.other_images.attached? %>
  <% @photo.other_images.each do |other| %>
      <%= image_tag other %>
    <% end %>
<% end %>

<%= link_to 'Edit', edit_photo_path(@photo) %> |
<%= link_to 'Back', photos_path %>
